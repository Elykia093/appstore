name: Renovate

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - main
    paths:
      - "apps/**/docker-compose.yml"
      - "renovate.json"
      - ".github/workflows/renovate.yml"
  workflow_dispatch:
    inputs:
      manual-trigger:
        description: 'Manually trigger Renovate'
        default: ''

concurrency:
  group: renovate
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  renovate:
    runs-on: ubuntu-latest
    env:
      LOG_LEVEL: debug
      RENOVATE_REPOSITORIES: ${{ github.repository }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Check Renovate token mode
        env:
          HAS_RENOVATE_TOKEN: ${{ secrets.RENOVATE_TOKEN != '' }}
          HAS_MERGE_ADMIN_TOKEN: ${{ secrets.MERGE_ADMIN_TOKEN != '' }}
        run: |
          if [[ "$HAS_RENOVATE_TOKEN" == "true" || "$HAS_MERGE_ADMIN_TOKEN" == "true" ]]; then
            echo "Renovate will use a token that can trigger downstream workflows."
          else
            echo "::warning::Renovate is falling back to GITHUB_TOKEN. Configure RENOVATE_TOKEN or MERGE_ADMIN_TOKEN if renovate/* branch pushes must trigger renovate-app-version.yml automatically."
          fi
      - name: Run Renovate
        uses: renovatebot/github-action@v43.0.7
        with:
          token: ${{ secrets.RENOVATE_TOKEN || secrets.MERGE_ADMIN_TOKEN || secrets.GITHUB_TOKEN }}
